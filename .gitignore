# Python bytecode cache (never commit — causes binary merge conflicts)
__pycache__/
*.py[cod]
*.egg-info/

# Virtualenvs
.venv/
venv/

# Qt/CMake build output (regenerated by `cmake --build`; never commit)
qt_demo/build/
